# 手写数字识别网站功能说明

## 项目定位

本网站用于识别 `0–9` 的单个手写数字。系统基于 Flask 提供网页服务，使用 ONNX Runtime 加载由 PyTorch 模型转换得到的 MNIST 卷积神经网络模型，以降低线上推理的内存占用。

当前版本只包含以下三项识别功能，不包含图片剪裁、亮度调整、颜色变换、账号登录或历史记录等功能。

## 1. 单张图片识别

用户可以上传一张 PNG 图片，系统完成预处理后调用模型预测，并在结果页展示：

- 上传图片的缩略图
- 模型预测的数字
- 从文件名提取到的真实标签（如果存在）
- 预测是否正确及对应准确率

## 2. 批量图片识别

用户可以一次选择多张 PNG 图片，系统逐张完成识别，并通过表格展示：

- 图片缩略图和文件名
- 每张图片的预测数字
- 从文件名提取的真实标签
- 每张图片是否识别正确
- 所有带标签图片的总体准确率

文件名中的第一个数字会作为真实标签。例如，`test_5.png` 的真实标签为 `5`。没有数字标签的文件仍可识别，但不会参与准确率统计。

## 3. 网页手写输入识别

用户可以直接在网页画布中书写数字：

- 支持鼠标书写
- 支持触摸屏书写
- 点击识别按钮后立即返回预测数字
- 可以清空画布后重新书写

## 识别范围

- 支持内容：`0–9` 的单个手写数字
- 支持上传格式：PNG
- 不支持：汉字、英文字母、多位数字和通用文字识别

## 页面与接口

| 地址 | 功能 |
| --- | --- |
| `/` | 首页和功能入口 |
| `/upload` | 单张 PNG 图片识别 |
| `/batch` | 多张 PNG 图片批量识别 |
| `/draw` | 网页画布手写识别 |
| `/uploads/<filename>` | 展示已上传的图片 |

## 后续可选改进

- 增加 JPG、JPEG 等图片格式支持
- 改进图片居中、缩放和二值化预处理
- 在用户停止书写后自动触发识别
- 增加置信度和各数字概率展示
- 增加异常处理与上传大小限制
